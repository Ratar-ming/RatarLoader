# RatarLoader
A loader that uses a special kernel boot protocol to start your operating system kernel. You can use it to boot any kernel – as long as it supports this boot protocol and meets the kernel's startup requirements.
